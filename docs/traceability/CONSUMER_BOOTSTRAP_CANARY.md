# Consumer Bootstrap Canary — Issue #52 / skills-shared #366

This is a consumer-owned evidence projection for the first real public `DOMAIN-DECOUPLING-V1` bootstrap canary. It is navigation and handoff, not a replacement for GitHub workflow metadata, generated machine bindings, or receipts.

## Frozen subjects

```text
consumer repository  ed3c/website-design-compiler
consumer base        4a79d9635911690950f02edda4505672ba7544f6
consumer base tree   9e524b79e19d7b6792484de786afdcddd2407625
consumer branch      integration/52-domain-decoupling-bootstrap-v1
shared repository    ed3c/skills-shared
shared subject       ccef97dedd7ea8b1873e3afa130ca82b8eabb413
```

The exact current consumer head is GitHub PR #53 metadata. Do not copy a mutable head value here and treat it as authority.

## Execution denominator

### Attempt 1 — first red retained

```text
head before writer run  85fedac8691a70f7b7ecf67a02a619904ce0382a
workflow                Consumer Bootstrap Apply Once
run                     32249743790
canonical --apply       PASS
publish generated bytes FAIL
```

Failure was an external GitHub permission boundary: the Actions token had `contents: write` but not the workflow-writing permission needed to push `.github/workflows/domain-decoupling-bootstrap.yml`. No bootstrap constraint was weakened.

### Attempt 2 — split integration authority

```text
repair commit            56d6233da046592a7d734918b1bb8b3c1c845b2f
workflow run             32249991434
canonical --apply        PASS
non-workflow bytes push  PASS
```

The temporary writer removed the generated workflow from its own commit. The GitHub connector, which owns workflow-file mutation in this integration lane, later installed the exact canonical workflow bytes.

### Attempt 3 — byte-drift red retained

```text
candidate                40fc3bb1cd386aa20d45ebb6ad50b1722d3f4fbc
workflow                 Domain Decoupling Consumer Bootstrap
run                      32250148218
bootstrap --check        FAIL
runtime observer         SKIPPED
```

The connector-created workflow omitted canonical explanatory comments. `consumer_bootstrap.py --check` correctly rejected the byte mismatch as `generated bootstrap workflow drifted`.

### Attempt 4 — exact canonical bytes

```text
workflow blob            731e6292c19fbbe1066a84b555a3657cce1884ee
candidate before handoff fcc0d1910a65f869398580877084745f33640f89
workflow run             32250354059
bootstrap --check        PASS
runtime observer         PASS
```

The runtime observer reached the task-bound `consumer-bootstrap-verification/v1` admission path. This is hosted read-only bootstrap-verification evidence only.

## Final path lease

The PR contains only canonical bootstrap-generated consumer routes plus this consumer-owned evidence packet:

```text
.agents/README.md
.agents/bindings/repository-control-plane.json
.agents/control-plane/README.md
.agents/control-plane/bootstrap-receipt.json
.agents/control-plane/source.json
.github/workflows/domain-decoupling-bootstrap.yml
AGENTS.md
ARCHITECTURE.md
CLAUDE.md
CONTEXT.md
README.md
docs/INDEX.md
docs/architecture/AGENTS.md
docs/architecture/DOCUMENT_ROUTING.md
docs/architecture/DOMAIN_DECOUPLING.md
docs/architecture/STATE_MACHINES.md
docs/integration/CROSS_REPO_INTEGRATION.md
docs/traceability/TRACEABILITY_INDEX.md
docs/traceability/CONSUMER_BOOTSTRAP_CANARY.md
```

The temporary `consumer-bootstrap-apply-once.yml` writer is absent from the final candidate. PR #44 implementation paths remain outside this lease.

## Evidence ceiling

```text
canonical public consumer --apply              PASS
exact generated byte readback                  PASS
hosted consumer bootstrap --check              PASS
GitHub Actions pinned-bundle runtime admission PASS
local Codex Skill discovery                    NOT_EXERCISED
local Claude Skill discovery                   NOT_EXERCISED
Agent/model repository task execution          NOT_EXERCISED
provider/private endpoint execution            NOT_EXERCISED
Git Town / Forgejo delivery                    NOT_EXERCISED
consumer product correctness                   NOT_CLAIMED
merge                                          HUMAN_ADMIT_REQUIRED
release                                        HUMAN_ADMIT_REQUIRED
rollback execution                             NOT_PERFORMED
```

## Local Handoff Execution Queue

1. Wait for the exact-head `Domain Decoupling Consumer Bootstrap` and repository-native `compiler-core` checks on the current PR head.
2. If either deciding check is red, preserve the run in the denominator and repair only the failing predicate.
3. If exact-head checks are green, keep PR #53 open for Human/repository review. Do not auto-merge.
4. After an admitted merge, read back the exact merge commit/tree and the generated workflow on `main`.
5. Record the admitted consumer evidence back to `ed3c/skills-shared#366`.
6. Run local Codex and Claude user-surface discovery as separate future evidence lanes; neither may be inferred from GitHub Actions PASS.

## Shadow Architect refusals

Reject any attempt to remove failed attempts from this denominator, preserve the temporary write-capable workflow, manually edit generated binding/receipt bytes, widen repository visibility/access/license/secret/provider authority, claim Agent/model execution, or proxy PR #44/product correctness with this bootstrap evidence.
